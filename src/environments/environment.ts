/**
 * There is no backend behind this build. `ScreenDataService`,
 * `AuthService` and `MockTemplatesService` fabricate and persist data to
 * `localStorage` directly — see `DEVELOPMENT.md` for why.
 */
export const environment = {
  production: false,
};
